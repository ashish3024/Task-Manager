# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Ubuntu@012
DB_NAME=task_manager
DB_INIT_FILE=./init.sql

# To initialize the database, run: mysql -u $DB_USER -p $DB_PASSWORD < $DB_INIT_FILE
# Or keep the SQL in DB_INIT_SQL (single-line) if you prefer to run it from app code.
# DB_INIT_SQL="CREATE DATABASE task_manager; USE task_manager; CREATE TABLE tasks (id INT AUTO_INCREMENT PRIMARY KEY, title VARCHAR(255) NOT NULL, description TEXT, status ENUM('Pending','In Progress','Completed') DEFAULT 'Pending', created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);"